{
  "name": "alexa-router",
  "version": "1.0.0",
  "description": "Easily build custom skills for Alexa",
  "scripts": {
    "test": "standard && lab --verbose --colors --assert code",
    "test-cov": "standard && npm test -- -r console -o stdout -r html -o coverage/coverage.html -r lcov -o coverage/lcov.info"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/estate/alexa-router.git"
  },
  "keywords": [
    "amazon",
    "alexa",
    "custom",
    "skill",
    "framework",
    "route",
    "action",
    "routing"
  ],
  "author": "Estate <team@tryestate.com>",
  "license": "MIT",
  "devDependencies": {
    "co": "^4.6.0",
    "code": "^3.0.1",
    "lab": "^10.9.0",
    "standard": "^7.1.2"
  },
  "dependencies": {
    "joi": "^9.0.0",
    "lodash": "^4.13.1",
    "request": "^2.73.0",
    "request-promise": "^4.0.2",
    "x509": "^0.2.6"
  }
}
